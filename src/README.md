# Deprecated

This folder is no longer used. The monorepo lives in:

- `apps/marketing` — public website
- `apps/super-admin` — Super Admin portal
- `apps/operator` — Operator portal + API
- `packages/*` — shared code

See [ARCHITECTURE.md](../ARCHITECTURE.md).
