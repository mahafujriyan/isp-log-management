/** @deprecated Use `@/auth` — backward-compatible re-export for PHASE 5 docs */
export { auth, handlers, signIn, signOut } from "@/auth";
export { auth as edgeAuth } from "@/auth.edge";
export { authenticateUser } from "@/services/auth.service";
export { nextAuthConfig } from "@/auth.config";
