/** @deprecated Use `@/auth` — backward-compatible re-export for PHASE 5 docs */
export { auth, handlers, signIn, signOut } from "@isp/auth";
export { auth as edgeAuth } from "@isp/auth/edge";
export { authenticateUser } from "@isp/core/services/auth.service";
export { nextAuthConfig } from "@isp/auth/config";
