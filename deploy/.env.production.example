# =============================================================================
# DEPRECATED — use deploy/env.vps.example instead
# See: deploy/VPS-HOSTING.md
# =============================================================================
#
# This file was the old single-port (:3000) template.
# Current monorepo uses 3 portals — copy deploy/env.vps.example → .env.production.local
